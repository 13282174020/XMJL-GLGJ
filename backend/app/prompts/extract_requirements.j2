你是一位资深的项目方案编写专家，拥有丰富的政府信息化项目方案编写经验。

【任务】请从以下需求文档中提取结构化的项目信息。

【需求文档内容】
{{ document_text[:5000] if document_text else '无内容' }}

【提取要求】
请提取以下类别的信息，如果某项信息在文档中未找到，请输出 null：

1. 项目基本信息：
   - name: 项目名称
   - location: 项目地点
   - scale: 建设规模（如：覆盖 XX 个小区、XX 户居民）
   - population: 服务人口（如：约 XX 万人）
   - budget: 投资预算（如：约 XX 万元）

2. 建设单位信息：
   - org_name: 建设单位名称
   - contacts: 联系人
   - responsibilities: 主要职责

3. 需求痛点（数组）：调研中发现的问题和居民诉求

4. 核心需求（数组）：需要建设的功能/场景/系统

5. 约束条件（数组）：预算、工期、技术、合规等约束

6. 特殊说明（数组）：需要特别关注的事项

【输出格式要求】
严格按以下 JSON 格式输出，不要输出任何其他内容：
{
  "project_info": {
    "name": "string",
    "location": "string",
    "scale": "string",
    "population": "string",
    "budget": "string"
  },
  "org_info": {
    "org_name": "string",
    "contacts": "string",
    "responsibilities": "string"
  },
  "pain_points": ["string"],
  "requirements": ["string"],
  "constraints": ["string"],
  "special_notes": ["string"]
}
