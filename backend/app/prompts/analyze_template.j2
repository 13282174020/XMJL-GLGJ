你是一位专业的文档结构分析专家。

【任务】请分析以下模板文档的章节结构，识别每个章节的层级、编号规则、内容类型和预期字数。

【模板文档内容】
{{ document_text[:5000] if document_text else '无内容' }}

【分析要求】
请识别以下信息：

1. 章节层级结构（章、节、小节）
2. 每个章节的编号规则（如：第一章、1.1、1.1.1）
3. 内容类型判断（text=纯文本，list=列表，table=表格，mixed=混合）
4. 预期字数估算

【输出格式要求】
严格按以下 JSON 格式输出：
{
  "chapters": [
    {
      "number": "第一章",
      "title": "章节标题",
      "level": 1,
      "content_type": "text",
      "estimated_words": 1000,
      "subsections": [
        {
          "number": "1.1",
          "title": "小节标题",
          "level": 2,
          "content_type": "text",
          "estimated_words": 300
        }
      ]
    }
  ]
}

【注意事项】
1. level 字段：章=1，节=2，小节=3
2. content_type 根据小节标题和内容特征判断
3. estimated_words 根据章节重要性估算
